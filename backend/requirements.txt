# FastAPI and Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.6

# Database
supabase==2.9.1
asyncpg==0.29.0
sqlalchemy==2.0.25

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
python-dotenv==1.0.0
email-validator==2.1.0

# AI & LLM
openai==1.54.0
langchain==0.3.7
langchain-openai==0.2.8
langchain-core==0.3.17
anthropic==0.39.0
tiktoken==0.8.0

# Vector Database & Embeddings
# NOTE: disabled for local Windows dev — chroma-hnswlib needs a C++ compiler
# (MSVC build tools) that isn't installed, and sentence-transformers pulls a
# ~2GB torch download. These are imported lazily in app/utils/vector_store.py,
# so the API runs fine without them; only document-vector/RAG features are off.
# chromadb==0.5.15
# sentence-transformers==3.3.1
# chroma-hnswlib==0.7.6

# Document Processing
PyPDF2==3.0.1
pdfplumber==0.11.9
openpyxl==3.1.5
pandas==2.2.3
Pillow==11.0.0

# PDF Generation (HTML -> PDF). WeasyPrint needs native libs (pango, cairo,
# gdk-pixbuf); it is imported lazily so a missing lib won't crash the API.
weasyprint==62.3
jinja2==3.1.4

# Redis & Caching
redis==5.0.1
celery==5.3.6

# Data Validation
pydantic==2.10.3
pydantic-settings==2.6.1

# HTTP & Networking
httpx>=0.27,<0.28
aiohttp==3.10.10

# Utilities
python-dateutil==2.9.0
pytz==2024.2
typing-extensions==4.12.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# CORS
fastapi-cors==0.0.6

# File Storage
boto3==1.34.34

# Monitoring (optional)
prometheus-client==0.19.0

# Market data + payment SDKs (imported by app code; were missing — added so a
# fresh deploy boots without ModuleNotFoundError)
yfinance==1.5.1
stripe==15.3.1
razorpay==2.0.1
